version: '3.8'

services:
  postgres:
    image: postgres:latest
    container_name: nubankdb
    restart: always
    environment:
      POSTGRES_DB: nubankdb
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"